/**
 * Optional Helius extension — `import { HeliusRpcProvider } from "solanaxph-sdk/helius"`.
 * Kept out of the main entry so the core SDK never depends on a vendor.
 */
export * from "./providers/helius/index.js";
